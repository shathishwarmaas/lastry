pipeline {
    agent any

    stages {
        stage('GIT PULL') {
            steps {
                git branch: "love", url: 'https://github.com/
            }
        }
        stage('TEST') {
            steps {
                sh 'flutter test'
            }
        }
        stage('BUILD') {
            steps {
                sh '''
                  #!/bin/sh
                  flutter build apk --debug
                  '''
            }
        }
       
    }
}
